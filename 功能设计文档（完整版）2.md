# 统一数据展示系统 — 完整功能设计文档 v2

---

## 概述

本软件的核心目标是实现结构化与非结构化数据的统一存储与多形态展示。
通过"一份数据，多种展示"的理念，用户可以将同一份数据自由切换为**大纲、思维导图、文档、表格、幻灯片**五种展示方式。
取代当前 Word、Excel、PPT、结构化数据库、非结构化数据库等工具数据难以互通的困境。

**核心原理**：所有内容以嵌套键值对形式存储，通过键值对到不同排版方式的映射规则，实现展示方式的自由变换。键名自动生成，用户无需关心底层数据结构。

---

## 目录

1. [数据模型设计](#1-数据模型设计)（原始数据、底层数据、转义表）
2. [完整键字段定义](#2-完整键字段定义)
3. [五种展示方式的详细设计](#3-五种展示方式的详细设计)
4. [数据输入方式设计](#4-数据输入方式设计)
5. [产品形态与交互设计](#5-产品形态与交互设计)
6. [文件格式与存储设计](#6-文件格式与存储设计)
7. [多端协同与未来规划](#7-多端协同与未来规划)

---

## 1 数据模型设计

本系统有三层数据：**原始数据、底层数据、转义表**。三者关系如下：

```
原始数据（人可读）  ←→  转义表  ←→  底层数据（压缩存储）
```

- 打开文件时：底层数据 + 转义表 → 还原为原始数据
- 保存文件时：原始数据 + 转义表 → 压缩为底层数据
- 编辑过程中：始终操作原始数据，不触发转义，保证可读性和性能

---

### 1.1 原始数据

原始数据是用户可直接阅读的嵌套键值对结构，JSON 格式。

核心设计：以多级标题键（t1-1, t2-1, t3-1...）构成树形层级，每个节点包含内容和格式属性。

#### 键名命名规则

| 层级 | 键名格式 | 示例 |
|------|----------|------|
| 一级标题 | `t1-序号` | t1-1, t1-2, t1-3 ... |
| 二级标题 | `t2-序号` | t2-1, t2-2 ...（嵌套在一级标题内部） |
| 三级标题 | `t3-序号` | t3-1, t3-2 ...（嵌套在二级标题内部） |
| N级标题 | `tN-序号` | 理论上支持无限层级 |

- 第一个数字表示**层级**，第二个数字表示**序号**
- 键名由系统自动生成，用户新增标题时自动分配下一个序号
- 删除整个键值对时会出现键名不连续，保存文件时可选整体重排，也可保留空缺

#### 逐字富文本值语法

字体属性字段（如 bold、color、font 等**字符级**样式字段）在保留原有整体赋值的基础上，新增**逐字覆盖语法**，允许对文本中特定字符范围设置不同样式。

**值格式**：

```
简单形式：value
逐字形式："base_value;override_value(start,end)(start,end);override_value2(start,end)..."
```

- `base_value` — 基础值，对全部字符生效
- `;` — 分隔符，后续为覆盖段
- `override_value(start,end)(start,end)` — 覆盖值及其作用的字符范围（1-based，两端闭合）
- 同一覆盖值可跟随多个范围括号
- 多个不同覆盖值用 `;` 分隔

**示例**：

```jsonc
"bold": 1                              // 全部加粗（简单形式）
"bold": "1;0(2,3)(5,10)"              // 全部加粗，但第2-3个字和第5-10个字不加粗
"color": "0,0,0;255,0,0(1,5)"        // 默认黑色，第1-5个字红色
"color": "0,0,0;255,0,0(1,5);0,0,255(10,15)"  // 默认黑色，1-5红色，10-15蓝色
"font": "微软雅黑;宋体(3,8)"          // 默认微软雅黑，第3-8个字用宋体
"font_size": "14;18(1,3)"            // 默认14pt，第1-3个字18pt
```

**适用字段**（所有字符级样式字段）：

> font, font_size, color, bold, italic, underline, underline_color, strikethrough,
> superscript, subscript, highlight, letter_spacing, text_shadow, text_outline,
> small_caps, font_scale

**不适用字段**（段落级样式字段）：

> align, vertical_align, paragraph_before/after/line, paragraph_first_indent,
> indent_left/right, text_direction, word_wrap 等

**作用范围**：

- 在**节点级**使用时，作用于该节点的 `content` 文本
- 在 **type_global** 中使用时，作用于所有匹配节点
- 若需对 `body` 文本使用逐字样式，使用 `body.` 前缀（如 `"body.bold": "1;0(2,3)"`）

**解析规则**：

- 值为数字或不含 `;` 的字符串时，视为简单形式
- 值为包含 `;` 的字符串时，按 `;` 分割：第一段为基础值，后续段为覆盖段
- 每个覆盖段中，第一个 `(` 之前的部分为覆盖值，所有 `(start,end)` 为字符范围

---

#### 完整的原始数据示例

```json
{
  "meta": {
    "title": "文档标题",
    "author": "作者名",
    "created": "2026-02-11T10:00:00Z",
    "modified": "2026-02-11T14:00:00Z",
    "version": "1.0"
  },

  "type_global": {
    "t1-*.font": "微软雅黑",
    "t1-*.font_size": 24,
    "t1-*.color": "0,0,0",
    "t1-*.bold": 1,
    "t1-*.align": "left",
    "t1-*.paragraph_before": 1,
    "t1-*.paragraph_after": 0.5,
    "t1-*.paragraph_line": 1.5,
    "t1-*.indent_left": 0,

    "t2-*.font": "微软雅黑",
    "t2-*.font_size": 18,
    "t2-*.color": "50,50,50",
    "t2-*.bold": 1,
    "t2-*.paragraph_before": 0.5,
    "t2-*.paragraph_after": 0.3,
    "t2-*.paragraph_line": 1.5,

    "t3-*.font": "微软雅黑",
    "t3-*.font_size": 14,
    "t3-*.color": "80,80,80",
    "t3-*.paragraph_line": 1.3,

    "body.font": "宋体",
    "body.font_size": 12,
    "body.color": "0,0,0",
    "body.paragraph_line": 1.5,
    "body.paragraph_first_indent": 2,

    "hide_t": "t>3",
    "numbering_style": "1.1.1"
  },

  "t1-1": {
    "content": "第一章 项目概述",
    "bold": "1;0(4,8)",
    "t2-1": {
      "content": "项目背景",
      "t3-1": {
        "content": "行业现状分析",
        "body": "当前市场上的文档工具各自为政，Word处理文字，Excel处理表格，PPT处理演示..."
      },
      "t3-2": {
        "content": "痛点总结",
        "body": "数据在不同工具间难以流转，格式转换损失严重..."
      }
    },
    "t2-2": {
      "content": "项目目标",
      "hide": 1,
      "hide_body": 0,
      "body": "实现一份数据{{\"image\":\"assets/goal.png\",\"image.width\":\"80%\",\"image.align\":\"center\"}}的多种展示方式"
    }
  },

  "t1-2": {
    "content": "第二章 技术方案",
    "color": "0,80,160",
    "t2-1": {
      "content": "数据模型设计",
      "body": "采用嵌套键值对结构..."
    },
    "t2-2": {
      "content": "引用示例",
      "body": "=t1-1.t2-1.t3-1.body"
    }
  }
}
```

> **示例说明**：`"bold": "1;0(4,8)"` 表示 `t1-1` 的 content「第一章 项目概述」整体加粗，但第 4-8 个字（"项目概述"）不加粗。

---

#### 关键设计说明

**a) 样式继承机制**（优先级从低到高）：

```
type_global 全局样式 → 父节点样式 → 节点自身样式
```

- 例如：t1-1 未定义 font 时，使用 type_global 中 `t1-*.font` 的值"微软雅黑"
- 若 t1-1 自身定义了 `font: "黑体"`，则覆盖全局值
- 逐字富文本语法的值同样参与继承，节点自身的逐字值覆盖全局逐字值

**b) 内容引用机制**：

- 值以 `=` 开头表示引用，支持：
  - 本文件引用：`=t1-2.content`（引用本文件中 t1-2 的 content）
  - 跨文件引用：`=路径/文件名.t1-2.content`（引用其他文件的内容）
  - 引用是动态的，源数据修改后引用处自动更新

**c) 内嵌媒体语法**：

在 content 或 body 中使用 `{{...}}` 嵌入媒体：

```json
{{"image":"路径","image.width":"50%","image.align":"center","image.angle":0}}
{{"video":"路径或URL","video.width":"100%"}}
{{"audio":"路径"}}
```

媒体属性可通过原始数据修改，也可通过鼠标拖拽修改。

**d) body 字段**：

- 标题节点的正文内容，与 content（标题文字）分离
- 值可以为空字符串 `""`
- 在大纲/思维导图中通过 `hide_body` 字段控制是否显示
- 在文档模式中渲染为标题下方的段落文字
- 支持逐字富文本样式（通过 `body.*` 前缀字段设置）

**e) content 字段**：

- 每个 t 节点必须有此字段
- 值可以为空字符串 `""`，展示效果为空行/空节点
- 支持逐字富文本样式（通过节点级样式字段直接设置）

---

### 1.2 底层数据

底层数据是原始数据经过转义表压缩后的存储格式，用于减小文件体积。

**压缩规则**：

- 所有键名通过转义表映射为短键名
- 值不做压缩，保持原样
- 嵌套结构保持不变

**压缩后示例**：

```json
{
  "m": {
    "tt": "文档标题",
    "au": "作者名",
    "cr": "2026-02-11T10:00:00Z",
    "md": "2026-02-11T14:00:00Z",
    "v": "1.0"
  },
  "tg": {
    "t1-*.f": "微软雅黑",
    "t1-*.fs": 24,
    "t1-*.c1": "0,0,0",
    "t1-*.bd": 1
  },
  "t1-1": {
    "c": "第一章 项目概述",
    "bd": "1;0(4,8)",
    "t2-1": {
      "c": "项目背景",
      "t3-1": {
        "c": "行业现状分析",
        "b": "当前市场上的文档工具各自为政..."
      }
    }
  }
}
```

---

### 1.3 转义表

转义表是原始数据键名与底层数据短键名之间的双向映射字典。

**转义表结构**：

```json
{
  "version": "1.0",
  "mapping": {
    "meta": "m",
    "title": "tt",
    "author": "au",
    "created": "cr",
    "modified": "md",
    "version": "v",
    "type_global": "tg",
    "content": "c",
    "body": "b",
    "font": "f",
    "font_size": "fs",
    "color": "c1",
    "bold": "bd",
    "italic": "it",
    "underline": "ul",
    "underline_color": "ulc",
    "strikethrough": "st",
    "superscript": "sup",
    "subscript": "sub",
    "highlight": "hl",
    "letter_spacing": "ls",
    "text_shadow": "ts",
    "text_outline": "to",
    "small_caps": "sc",
    "font_scale": "fsc",
    "align": "al",
    "vertical_align": "va",
    "paragraph_before": "pb",
    "paragraph_after": "pa",
    "paragraph_line": "pl",
    "paragraph_line_fixed": "plf",
    "paragraph_first_indent": "pfi",
    "indent_left": "il",
    "indent_right": "ir",
    "indent_hanging": "ih",
    "text_direction": "td",
    "word_wrap": "ww",
    "keep_together": "kt",
    "keep_with_next": "kwn",
    "page_break_before": "pbb",
    "widow_control": "wc",
    "hide": "h",
    "hide_t": "ht",
    "hide_body": "hb",
    "visible": "vis",
    "locked": "loc",
    "read_only": "ro",
    "collapsed_default": "cd",
    "numbering_style": "ns",
    "numbering_format": "nf",
    "numbering_start": "nst",
    "list_style": "lst",
    "background_color": "bgc",
    "background_opacity": "bgo",
    "border": "bdr",
    "border_style": "bdrs",
    "border_color": "bdrc",
    "border_width": "bdrw",
    "border_radius": "bdrr",
    "border_top": "bdrt",
    "border_bottom": "bdrb",
    "border_left": "bdrl",
    "border_right": "bdrri",
    "shadow": "shd",
    "opacity": "op",
    "rotation": "rot",
    "image": "img",
    "image.width": "img.w",
    "image.height": "img.h",
    "image.align": "img.al",
    "image.angle": "img.ag",
    "image.border": "img.bdr",
    "image.caption": "img.cap",
    "image.float": "img.fl",
    "video": "vid",
    "video.width": "vid.w",
    "video.height": "vid.h",
    "video.autoplay": "vid.ap",
    "video.loop": "vid.lp",
    "video.poster": "vid.ps",
    "audio": "aud",
    "audio.autoplay": "aud.ap",
    "audio.loop": "aud.lp",
    "link": "lk",
    "link.text": "lk.t",
    "link.target": "lk.tg",
    "note": "nt",
    "comment": "cmt",
    "cell_formula": "cf",
    "cell_format": "cfm",
    "cell_decimal": "cfd",
    "cell_merge": "cm",
    "cell_wrap": "cw",
    "cell_shrink": "csk",
    "cell_rotation": "crt",
    "cell_locked": "clk",
    "cell_hidden": "chd",
    "column_width": "colw",
    "row_height": "rwh",
    "freeze_row": "fzr",
    "freeze_col": "fzc",
    "slide_break": "sb",
    "slide_layout": "sl",
    "slide_width": "sw",
    "slide_height": "sh",
    "slide_padding": "sp",
    "slide_background": "sbg",
    "slide_background_image": "sbgi",
    "slide_transition": "str",
    "slide_transition_duration": "strd",
    "slide_hide_level": "shl",
    "slide_notes": "sn",
    "mindmap_layout": "mml",
    "mindmap_line_style": "mmls",
    "mindmap_line_color": "mmlc",
    "mindmap_line_width": "mmlw",
    "mindmap_node_shape": "mmns",
    "mindmap_node_gap_h": "mmgh",
    "mindmap_node_gap_v": "mmgv",
    "mindmap_color_scheme": "mmcs"
  }
}
```

**管理规则**：

- 转义表存放在软件安装目录中，所有项目共用同一份
- 用户可在项目文件夹中放置自定义转义表，覆盖默认映射
- 转义表支持版本号，便于后续扩展新字段时保持向后兼容
- 复合键（含 `.` 的键名，如 `body.font`）由系统自动拆分各段分别映射（`body` → `b`，`font` → `f`，结果为 `b.f`），无需在转义表中逐一列举

---

## 2 完整键字段定义

以下是系统中所有可用的键字段，按类别分组。
字段参考了 Word、Excel、PPT 的核心排版属性，并根据本系统的统一数据模型进行了简化和整合。
字段可出现在 `type_global`（全局样式）中，也可出现在具体节点中（覆盖全局值）。

---

### 2.1 元信息字段（meta）

| 字段 | 说明 |
|------|------|
| title | 文档标题 |
| author | 作者 |
| created | 创建时间（ISO 8601 格式） |
| modified | 最后修改时间 |
| version | 文档版本号 |
| description | 文档描述/摘要 |
| tags | 标签列表（数组） |
| language | 文档语言（zh-CN, en-US 等） |

---

### 2.2 内容字段

| 字段 | 说明 |
|------|------|
| content | 标题文字。每个 t 节点必须有此字段；值可以为空字符串，展示为空行/空节点 |
| body | 正文内容。标题下方的段落文字，可选；值可以为空字符串 |
| note | 备注/批注。不在正式展示中显示，仅侧边栏可见 |
| comment | 评论。协作场景下的讨论内容 |

---

### 2.3 字体属性字段

> **所有字体属性字段均支持逐字富文本语法**（参见 [1.1 逐字富文本值语法](#逐字富文本值语法)），可对文本中特定字符范围设置不同样式。

| 字段 | 说明 | 逐字示例 |
|------|------|----------|
| font | 字体名称（如"微软雅黑"、"宋体"、"Arial"） | `"微软雅黑;宋体(3,8)"` |
| font_size | 字号（单位：磅，如 12、14、18、24） | `"14;18(1,3)"` |
| color | 字体颜色（RGB 格式："R,G,B"，如"0,0,0"为黑色） | `"0,0,0;255,0,0(1,5)"` |
| bold | 加粗（1=加粗，0=不加粗） | `"1;0(2,3)(5,10)"` |
| italic | 斜体（1=斜体，0=不斜体） | `"0;1(3,6)"` |
| underline | 下划线类型（0=无，1=单线，2=双线，3=虚线，4=波浪线） | `"0;1(1,5)"` |
| underline_color | 下划线颜色（RGB 格式，默认跟随字体颜色） | `"0,0,0;255,0,0(1,5)"` |
| strikethrough | 删除线（0=无，1=单线，2=双线） | `"0;1(3,8)"` |
| superscript | 上标（1=启用） | `"0;1(5,5)"` |
| subscript | 下标（1=启用） | `"0;1(6,6)"` |
| highlight | 高亮背景色（RGB 格式） | `";255,255,0(1,5)"` |
| letter_spacing | 字符间距（单位：磅，正值加宽，负值紧缩） | `"0;2(1,3)"` |
| text_shadow | 文字阴影（1=启用） | `"0;1(1,4)"` |
| text_outline | 空心字（1=启用） | `"0;1(1,4)"` |
| small_caps | 小型大写（1=启用，仅对西文有效） | `"0;1(1,4)"` |
| font_scale | 字符缩放（百分比，如 150 表示 150%） | `"100;150(1,3)"` |

**节点级与 body 级**：

- 节点级直接写字段名（如 `"bold": "1;0(2,3)"`），作用于 **content** 文本
- 使用 `body.` 前缀（如 `"body.bold": "1;0(2,3)"`），作用于 **body** 文本
- `body.*` 前缀字段可出现在 `type_global`（全局 body 样式）或节点级（单节点 body 样式覆盖）

---

### 2.4 段落属性字段

> 段落属性是块级属性，作用于整个段落，**不支持**逐字覆盖语法。

| 字段 | 说明 |
|------|------|
| align | 水平对齐（left=左对齐, center=居中, right=右对齐, justify=两端对齐, distribute=分散对齐） |
| vertical_align | 垂直对齐（top=顶端, center=居中, bottom=底端） |
| paragraph_before | 段前间距（单位：行，如 1 表示段前空1行） |
| paragraph_after | 段后间距（单位：行） |
| paragraph_line | 行间距（倍数，如 1.5 表示1.5倍行距） |
| paragraph_line_fixed | 固定行距（单位：磅，设置后 paragraph_line 失效） |
| paragraph_first_indent | 首行缩进（单位：字符数，如 2 表示缩进2个字符） |
| indent_left | 左缩进（单位：字符数） |
| indent_right | 右缩进（单位：字符数） |
| indent_hanging | 悬挂缩进（单位：字符数，与首行缩进互斥） |
| text_direction | 文本方向（ltr=从左到右, rtl=从右到左, vertical=竖排） |
| word_wrap | 自动换行（1=启用，0=禁用） |
| keep_together | 段中不分页（1=启用，文档模式下有效） |
| keep_with_next | 与下段同页（1=启用） |
| page_break_before | 段前分页（1=启用，文档模式下有效） |
| widow_control | 孤行控制（1=启用，防止段首/段尾单行出现在页面顶/底） |

---

### 2.5 显示控制字段

| 字段 | 说明 |
|------|------|
| hide | 子节点折叠（0=展开，1=折叠）。控制当前节点的**子节点**是否可见 |
| hide_body | 正文折叠（0=显示，1=折叠）。控制当前节点的 **body** 正文是否可见。在大纲模式和思维导图模式中，用户可通过点击切换折叠/展开状态 |
| hide_t | 标题序号隐藏规则（如 `"t>3"` 表示4级及以下标题不显示序号） |
| visible | 可见性（1=可见，0=隐藏不渲染）。与 hide 不同，这里是完全不显示，节点不占据空间 |
| locked | 锁定（1=锁定不可编辑） |
| read_only | 只读（1=只读） |
| collapsed_default | 默认折叠深度（数字，如 2 表示默认只展开到2级标题） |

**hide 与 hide_body 的区别**：

- `hide` 控制**子节点**（子树）的折叠状态，折叠后子节点全部隐藏
- `hide_body` 控制该节点自身的 **body 正文**的折叠状态，折叠后仅隐藏正文，标题（content）和子节点不受影响

---

### 2.6 编号与列表字段

| 字段 | 说明 |
|------|------|
| numbering_style | 编号样式（见下表） |
| numbering_format | 自定义编号格式（如 `"第{n}章"`，`{n}` 为占位符） |
| numbering_start | 起始编号（默认1） |
| list_style | 列表符号样式（bullet 模式下：disc=实心圆, circle=空心圆, square=方块, dash=短横线） |

**numbering_style 可选值**：

| 值 | 效果 |
|----|------|
| `"1.1.1"` | 1, 1.1, 1.1.1（阿拉伯数字层级） |
| `"一.1.1"` | 一, 1.1, 1.1.1（中文一级+数字） |
| `"I.A.1"` | I, A, 1（罗马+字母+数字） |
| `"bullet"` | 无序列表（●, ○, ■ 层级符号） |
| `"none"` | 不显示编号 |
| `"custom"` | 自定义（配合 numbering_format 使用） |

---

### 2.7 边框与背景字段

| 字段 | 说明 |
|------|------|
| background_color | 背景色（RGB 格式） |
| background_opacity | 背景透明度（0-100，0=全透明，100=不透明） |
| border | 边框开关（1=显示边框） |
| border_style | 边框线型（solid=实线, dashed=虚线, dotted=点线, double=双线） |
| border_color | 边框颜色（RGB 格式） |
| border_width | 边框宽度（单位：像素） |
| border_radius | 边框圆角（单位：像素，0=直角） |
| border_top | 单独控制上边框（格式同 border_style） |
| border_bottom | 单独控制下边框 |
| border_left | 单独控制左边框 |
| border_right | 单独控制右边框 |
| shadow | 阴影效果（格式：`"x偏移,y偏移,模糊半径,颜色"`，如 `"2,2,5,200,200,200"`） |

---

### 2.8 媒体嵌入字段

内嵌在 content 或 body 中，使用 `{{...}}` 语法。

**图片属性**：

| 字段 | 说明 |
|------|------|
| image | 图片路径（相对路径或绝对路径） |
| image.width | 宽度（百分比如 `"50%"`，或像素如 `"300px"`） |
| image.height | 高度（同上，默认按比例缩放） |
| image.align | 对齐方式（left, center, right） |
| image.angle | 旋转角度（0-360） |
| image.border | 边框（1=显示） |
| image.caption | 图片说明文字 |
| image.float | 文字环绕（none=独占行, left=左浮动, right=右浮动） |

**视频属性**：

| 字段 | 说明 |
|------|------|
| video | 视频路径或远端URL |
| video.width | 宽度 |
| video.height | 高度 |
| video.autoplay | 自动播放（1=启用） |
| video.loop | 循环播放（1=启用） |
| video.poster | 封面图路径 |

**音频属性**：

| 字段 | 说明 |
|------|------|
| audio | 音频路径 |
| audio.autoplay | 自动播放（1=启用） |
| audio.loop | 循环播放（1=启用） |

**链接属性**：

| 字段 | 说明 |
|------|------|
| link | 超链接URL |
| link.text | 链接显示文字（默认显示URL） |
| link.target | 打开方式（self=当前窗口, blank=新窗口） |

---

### 2.9 表格展示专用字段

以下字段用于表格（Excel-like）展示方式中的单元格配置：

| 字段 | 说明 |
|------|------|
| cell_formula | 单元格公式（如 `"t1-*.content"` 表示提取所有一级标题内容） |
| cell_format | 数值格式（number=数值, currency=货币, percent=百分比, date=日期, text=文本） |
| cell_decimal | 小数位数（默认2） |
| cell_merge | 合并单元格（格式：`"行数,列数"`，如 `"2,3"` 表示合并2行3列） |
| cell_wrap | 自动换行（1=启用） |
| cell_shrink | 缩小字体填充（1=启用） |
| cell_rotation | 文本旋转角度（0-180，255=竖排） |
| cell_locked | 锁定单元格（1=锁定） |
| cell_hidden | 隐藏公式（1=隐藏） |
| column_width | 列宽（单位：字符数） |
| row_height | 行高（单位：磅） |
| freeze_row | 冻结行数（如 1 表示冻结首行） |
| freeze_col | 冻结列数（如 1 表示冻结首列） |

---

### 2.10 幻灯片展示专用字段

以下字段用于幻灯片（PPT-like）展示方式：

| 字段 | 说明 |
|------|------|
| slide_break | 分页规则（`"t1"`=每个一级标题一页, `"t2"`=每个二级标题一页） |
| slide_layout | 页面布局（见下表） |
| slide_width | 页面宽度（单位：毫米，默认 254 即 16:9） |
| slide_height | 页面高度（单位：毫米，默认 143） |
| slide_padding | 页面内边距（单位：毫米） |
| slide_background | 页面背景色（RGB 格式） |
| slide_background_image | 页面背景图片路径 |
| slide_transition | 切换动画（fade=淡入, slide=滑动, zoom=缩放, none=无） |
| slide_transition_duration | 切换动画时长（单位：毫秒，默认 500） |
| slide_hide_level | 隐藏层级（数字，如 3 表示该页只显示到3级标题） |
| slide_notes | 演讲者备注 |

**slide_layout 可选值**：

| 值 | 说明 |
|----|------|
| `"title"` | 标题页（居中大标题+副标题） |
| `"content"` | 标题+内容（上方标题，下方正文） |
| `"two_column"` | 左右两栏 |
| `"two_row"` | 上下两栏 |
| `"grid_4"` | 四宫格 |
| `"grid_9"` | 九宫格 |
| `"blank"` | 空白页 |
| `"image_full"` | 全图页 |
| `"image_left"` | 左图右文 |
| `"image_right"` | 右图左文 |

---

### 2.11 思维导图展示专用字段

| 字段 | 说明 |
|------|------|
| mindmap_layout | 布局类型（见下表） |
| mindmap_line_style | 连线样式（curve=曲线, straight=直线, elbow=折线, rounded=圆角折线） |
| mindmap_line_color | 连线颜色（RGB 格式，默认跟随节点颜色） |
| mindmap_line_width | 连线宽度（单位：像素，默认 2） |
| mindmap_node_shape | 节点形状（rect=矩形, rounded=圆角矩形, ellipse=椭圆, diamond=菱形, none=无边框） |
| mindmap_node_gap_h | 节点水平间距（单位：像素） |
| mindmap_node_gap_v | 节点垂直间距（单位：像素） |
| mindmap_color_scheme | 配色方案（rainbow=彩虹色自动分配, mono=单色, custom=自定义） |

**mindmap_layout 可选值**：

| 值 | 说明 |
|----|------|
| `"right"` | 向右逻辑图（默认，根节点在左，分支向右展开） |
| `"left"` | 向左逻辑图 |
| `"down"` | 向下逻辑图 |
| `"up"` | 向上逻辑图 |
| `"balanced"` | 双向逻辑图（根节点居中，左右均衡展开） |
| `"org_chart"` | 组织架构图（自上而下层级展开） |
| `"tree_right"` | 树状图向右（子节点竖向排列） |
| `"tree_left"` | 树状图向左 |
| `"fishbone"` | 鱼骨图（因果分析） |
| `"timeline_h"` | 水平时间轴 |
| `"timeline_v"` | 垂直时间轴 |
| `"radial"` | 放射状/圆形（根节点在中心，子节点环绕） |

---

## 3 五种展示方式的详细设计

所有展示方式共享同一份原始数据，切换展示方式时不改变数据本身，只改变渲染规则。
每种展示方式都支持编辑，编辑操作直接修改原始数据，其他展示方式自动同步。

---

### 3.1 大纲模式（类似幕布）

#### 渲染规则

- 隐藏所有键名（t1-1, t2-1 等），仅展示 content 值（也可选择显示键名）
- 按层级缩进显示，每深一级增加固定缩进量（默认 24px）
- 每个节点左侧显示**折叠箭头**（▶ 折叠 / ▼ 展开），控制子节点显示，对应 `hide` 字段
- 有 body 内容的节点在 content 右侧显示 **body 折叠指示器**（▾ 展开 / ▸ 折叠），对应 `hide_body` 字段
- body 字段展开时显示在 content 下方，字号略小，颜色略浅
- 应用 type_global 和节点自身的字体、颜色、字号等样式
- 支持逐字富文本渲染：content 和 body 中的文字可按字符范围呈现不同样式
- 内嵌媒体（图片/视频）在节点下方内联渲染

#### 使用的字段

```
content, body, hide, hide_body,
font, font_size, color, bold, italic, underline,
background_color, highlight, note
（以上字符级字段均支持逐字富文本语法）
```

#### 不使用的字段

- 段落间距类字段（paragraph_before/after 等）在大纲模式下不渲染，因为大纲模式使用固定的行间距和缩进
- 表格专用、幻灯片专用字段不渲染

#### 交互操作

**键盘操作**：

| 按键 | 功能 |
|------|------|
| Enter | 在当前节点下方新增同级节点（自动生成键名） |
| Tab | 将当前节点降级（变为上方节点的子节点） |
| Shift+Tab | 将当前节点升级（变为父节点的同级节点） |
| 上/下箭头 | 在节点间移动光标 |
| Backspace | 删除当前节点（如有子节点，提示确认） |
| Ctrl+Z | 撤销 |
| Ctrl+Shift+Z | 重做 |

**鼠标操作**：

| 操作 | 功能 |
|------|------|
| 点击折叠箭头 | 展开/折叠子节点（修改 `hide` 字段） |
| 点击 body 折叠指示器 | 展开/折叠 body 正文（修改 `hide_body` 字段） |
| 拖拽节点 | 调整节点顺序或层级（拖到节点上方=同级排序，拖到节点右侧=变为子节点） |
| 双击节点 | 进入编辑模式 |
| 右键菜单 | 复制、剪切、粘贴、删除、添加子节点、添加备注、展开/折叠 body |

**工具栏操作**：

| 按钮 | 功能 |
|------|------|
| 字体选择器 | 修改选中节点的 font 字段 |
| 字号选择器 | 修改 font_size 字段 |
| 颜色选择器 | 修改 color 字段 |
| 加粗/斜体/下划线按钮 | 修改对应字段（选中部分文字时生成逐字富文本值） |
| 高亮按钮 | 修改 highlight 字段 |
| 插入图片按钮 | 在 content 中插入 `{{image:...}}` 语法 |
| 添加备注按钮 | 编辑 note 字段 |

> **逐字样式操作**：在大纲模式中选中 content 内的部分文字并设置格式时，系统自动将简单值转换为逐字富文本值。例如：原 `"bold": 1`，选中第 3-5 个字取消加粗后，自动变为 `"bold": "1;0(3,5)"`。

#### 视觉参考

```
▼ 第一章 项目概述                     ← 一级标题，大字号加粗
  ▼ 项目背景                          ← 二级标题，中字号
    ▶ 行业现状分析  ▾                 ← 三级标题（子树折叠），body 展开中
      当前市场上的文档工具各自为政...  ← body 内容，小字号浅色
    ● 痛点总结  ▸                     ← 三级标题，无子节点用圆点，body 折叠中
  ▶ 项目目标                          ← 二级标题，子树折叠（hide=1）
```

---

### 3.2 思维导图模式

#### 渲染规则

- 根节点（文档标题）居中显示，子节点按选定布局方式展开
- 默认隐藏键名（也可选择展示），仅展示 content 值
- 父子节点之间用连线连接，连线样式由 `mindmap_line_style` 控制
- 每个层级可自动分配不同颜色（`mindmap_color_scheme="rainbow"` 时）
- body 字段**默认折叠**（`hide_body` 默认为 1），节点上显示展开指示器，点击后在节点下方以小字展示 body 内容
- 内嵌媒体以缩略图形式显示在节点内
- 支持折叠/展开子树（对应 `hide` 字段）
- 支持逐字富文本渲染

#### 使用的字段

```
content, body, hide, hide_body,
font, font_size, color, bold, italic,
background_color, border, border_color, border_radius,
mindmap_layout, mindmap_line_style, mindmap_line_color, mindmap_line_width,
mindmap_node_shape, mindmap_node_gap_h, mindmap_node_gap_v,
mindmap_color_scheme
（字符级字段支持逐字富文本语法）
```

#### 交互操作

**键盘操作**：

| 按键 | 功能 |
|------|------|
| Enter | 新增同级节点 |
| Tab | 新增子节点 |
| Delete | 删除当前节点 |
| 空格键 | 折叠/展开子树 |
| 上/下/左/右 | 在节点间导航（方向取决于布局类型） |

**鼠标操作**：

| 操作 | 功能 |
|------|------|
| 单击节点 | 选中节点 |
| 双击节点 | 进入编辑模式 |
| 拖拽节点 | 调整节点位置或改变父子关系 |
| 点击折叠图标 | 折叠/展开子树 |
| 点击 body 展开指示器 | 展开/折叠 body 正文（修改 `hide_body` 字段） |
| 滚轮缩放 | 缩放画布 |
| 按住空格+拖拽 | 平移画布 |
| 右键菜单 | 添加子节点、添加同级节点、删除、复制、剪切、粘贴、更改节点样式、展开/折叠 body |

**工具栏操作**：

| 按钮 | 功能 |
|------|------|
| 布局切换 | 在不同布局类型间切换（向右、双向、组织架构等） |
| 连线样式 | 切换曲线/直线/折线 |
| 配色方案 | 切换彩虹色/单色/自定义 |
| 节点形状 | 切换矩形/圆角/椭圆等 |
| 导出为图片 | 将当前思维导图导出为 PNG/SVG |

#### 视觉参考（向右逻辑图布局）

```
                        ┌─ 行业现状分析
            ┌─ 项目背景 ┤
            │           └─ 痛点总结
项目概述 ───┤
            └─ 项目目标

                        ┌─ 数据模型设计
技术方案 ───┤
            └─ 引用示例
```

---

### 3.3 文档模式（类似 Word）

#### 渲染规则

- 键名转换为编号显示，格式由 `numbering_style` 控制
  - 例如 t1-1 → "1"，t2-1 → "1.1"，t3-1 → "1.1.1"
  - 或 t1-1 → "一"，t2-1 → "1.1"（中文一级+数字）
- content 渲染为标题文字，body 渲染为标题下方的正文段落
- `hide_body` 控制 body 是否显示；为 1 时正文段落不渲染
- 完整渲染段落格式：段前/段后间距、行距、首行缩进、对齐方式
- 完整渲染字体格式：字体、字号、颜色、加粗、斜体、下划线等
- **逐字富文本渲染**：同一段落内不同字符可呈现不同字体、颜色、粗细等样式
- 内嵌媒体按实际尺寸渲染，支持文字环绕（`image.float`）
- 支持分页渲染（`page_break_before`、`keep_together` 等）
- `hide_t` 控制标题序号的显示深度

#### 使用的字段

```
content, body, hide_body,
font, font_size, color, bold, italic, underline,
strikethrough, superscript, subscript, highlight, letter_spacing,
align, paragraph_before, paragraph_after, paragraph_line,
paragraph_first_indent, indent_left, indent_right,
numbering_style, numbering_format, hide_t,
background_color, border 系列, shadow,
page_break_before, keep_together, keep_with_next, widow_control,
所有媒体嵌入字段
（字符级字段支持逐字富文本语法）
```

#### 交互操作

**键盘操作**：

| 按键 | 功能 |
|------|------|
| Enter | 在正文中换行；在标题末尾按 Enter 新增同级标题 |
| Tab | 将标题降级（如 t1 变为 t2） |
| Shift+Tab | 将标题升级 |
| Ctrl+B/I/U | 加粗/斜体/下划线 |

**鼠标操作**：

| 操作 | 功能 |
|------|------|
| 选中文字+工具栏 | 修改字体、字号、颜色等格式（选中部分文字时自动生成逐字富文本值） |
| 拖拽图片边缘 | 调整图片尺寸（同步修改 image.width/height） |
| 右键菜单 | 段落格式、插入分页符、插入图片/表格 |

**工具栏操作**：

| 按钮 | 功能 |
|------|------|
| 标题级别选择器 | 快速设置当前段落为 H1-H6 |
| 编号样式选择器 | 切换 numbering_style |
| 段落格式面板 | 设置对齐、缩进、间距 |
| 插入菜单 | 图片、视频、链接、分页符、水平线 |

> **逐字样式操作**：在文档模式中选中部分文字并修改格式（如选中几个字改为红色），系统自动生成逐字富文本值，效果与 Word 中的局部格式设置一致。

#### 视觉参考

```
1 第一章 项目概述                     ← 一级标题，24pt 加粗
  1.1 项目背景                        ← 二级标题，18pt 加粗
    1.1.1 行业现状分析                ← 三级标题，14pt
      当前市场上的文档工具各自为政，   ← body 正文，12pt 宋体
    Word处理文字，Excel处理表格...       首行缩进2字符
    1.1.2 痛点总结
      数据在不同工具间难以流转...
  1.2 项目目标
      实现一份数据 [图片] 的多种展示方式
```

---

### 3.4 表格模式（类似 Excel）

#### 核心概念

表格模式的独特之处在于：单元格内容通过公式从原始数据中提取。用户在单元格中填写数据路径表达式，系统自动提取对应的值。

#### 数据路径表达式语法

**基础表达式**：

| 表达式 | 效果 |
|--------|------|
| `t1-*.content` | 提取所有一级标题的 content，纵向填充到下方单元格 |
| `t1-1.t2-*.content` | 提取 t1-1 下所有二级标题的 content |
| `t1-*.color` | 提取所有一级标题的 color 属性 |
| `t1-1.t2-1.body` | 提取特定节点的 body 值 |
| `t1-*.t2-*.content` | 二维展开（行=一级标题，列=二级标题） |

**高级表达式**：

| 表达式 | 效果 |
|--------|------|
| `t2-1.content.regex(/\d+/)` | 用正则从内容中捕获第一个数值 |
| `t1-*.content.count()` | 统计一级标题的数量 |
| `t1-*.body.length()` | 统计每个一级标题 body 的字符数 |

#### 渲染规则

- 表格初始为空白网格，用户在单元格中输入数据路径表达式
- 含通配符（`*`）的表达式自动向下/向右展开填充
- 单元格可直接输入静态文本，不使用表达式
- 单元格支持直接修改：修改后同步更新原始数据对应字段
- 应用 `cell_format` 进行数值格式化显示
- 支持合并单元格（与 Excel 不同：合并后每个单元格保持相同的值，例如 A1 和 A2 合并后，两者都持有相同的值）
- 支持冻结行列、自动换行

#### 使用的字段

```
content, body, font, font_size, color, bold, italic, underline,
align, vertical_align, background_color, border 系列,
cell_formula, cell_format, cell_decimal, cell_merge,
cell_wrap, cell_shrink, cell_rotation, cell_locked,
column_width, row_height, freeze_row, freeze_col
```

#### 交互操作

**键盘操作**：

| 按键 | 功能 |
|------|------|
| Tab | 移动到右侧单元格 |
| Shift+Tab | 移动到左侧单元格 |
| Enter | 移动到下方单元格 |
| F2 | 进入单元格编辑模式 |
| Ctrl+C/V/X | 复制/粘贴/剪切 |
| Delete | 清除单元格内容 |

**鼠标操作**：

| 操作 | 功能 |
|------|------|
| 单击单元格 | 选中 |
| 双击单元格 | 进入编辑模式 |
| 拖拽单元格边框 | 调整列宽/行高 |
| 拖拽选区右下角 | 自动填充（向下/向右复制表达式） |
| 右键菜单 | 插入行/列、删除行/列、合并单元格、设置格式 |

#### 视觉参考

```
|    A（t1-*.content）  |  B（t1-*.color）  |  C（自定义）  |
|----------------------|------------------|--------------|
| 第一章 项目概述       | 0,0,0            | 备注1        |
| 第二章 技术方案       | 0,80,160         | 备注2        |
```

---

### 3.5 幻灯片模式（类似 PPT）

#### 核心概念

幻灯片模式将原始数据按分页规则自动拆分为多页幻灯片。每页幻灯片从原始数据中提取对应层级的内容进行渲染。用户可为每页单独设置布局、背景、隐藏层级等。

#### 分页规则

由 `slide_break` 字段控制：

| 值 | 规则 |
|----|------|
| `"t1"` | 每个一级标题独占一页，其下的二级/三级标题作为页面内容 |
| `"t2"` | 每个二级标题独占一页 |
| `"t1+t2"` | 一级标题单独一页（作为章节页），二级标题各自一页 |

自动生成的页面结构示例（`slide_break="t1"` 时）：

- 第1页：t1-1 的 content 作为标题，t2-1/t2-2 的 content 作为内容列表
- 第2页：t1-2 的 content 作为标题，t2-1/t2-2 的 content 作为内容列表

#### 渲染规则

- 按 `slide_break` 规则将数据拆分为多页
- 每页应用 `slide_layout` 指定的布局模板
- `slide_hide_level` 控制每页显示的标题深度
- 标题渲染 content，正文渲染 body
- 内嵌媒体按布局区域渲染
- 支持页面切换动画（`slide_transition`）
- 每页可单独设置背景色/背景图
- 支持逐字富文本渲染

#### 布局模板详细说明

**title（标题页）**：

```
┌──────────────────────────┐
│                          │
│      [大标题居中]         │
│      [副标题居中]         │
│                          │
└──────────────────────────┘
```

**content（标题+内容）**：

```
┌──────────────────────────┐
│  [标题]                  │
│──────────────────────────│
│  · 内容项1               │
│  · 内容项2               │
│  · 内容项3               │
└──────────────────────────┘
```

**two_column（左右两栏）**：

```
┌────────────┬─────────────┐
│  [标题]                  │
│────────────┬─────────────│
│  左栏内容   │  右栏内容   │
│            │             │
└────────────┴─────────────┘
```

**two_row（上下两栏）**：

```
┌──────────────────────────┐
│  [标题]                  │
│──────────────────────────│
│  上栏内容                │
│──────────────────────────│
│  下栏内容                │
└──────────────────────────┘
```

**grid_4（四宫格）**：

```
┌────────────┬─────────────┐
│  区域1      │  区域2      │
│────────────┼─────────────│
│  区域3      │  区域4      │
└────────────┴─────────────┘
```

**image_left（左图右文）**：

```
┌────────────┬─────────────┐
│            │  [标题]      │
│  [图片]    │  · 内容项1   │
│            │  · 内容项2   │
└────────────┴─────────────┘
```

#### 交互操作

**键盘操作**：

| 按键 | 功能 |
|------|------|
| 左/右箭头 | 切换上一页/下一页 |
| F5 | 进入全屏演示模式 |
| Esc | 退出全屏演示 |
| 空格键 | 下一页（演示模式下） |

**鼠标操作**：

| 操作 | 功能 |
|------|------|
| 点击页面缩略图 | 跳转到指定页 |
| 双击页面内容 | 进入编辑模式 |
| 拖拽缩略图 | 调整页面顺序 |
| 右键菜单 | 更改布局、设置背景、插入新页、删除页 |

**工具栏操作**：

| 按钮 | 功能 |
|------|------|
| 布局选择器 | 切换当前页的布局模板 |
| 分页规则选择器 | 切换 slide_break 规则 |
| 背景设置 | 设置背景色或背景图片 |
| 切换动画选择器 | 设置页面切换效果 |
| 演讲者备注 | 编辑 slide_notes |

#### 演示模式

全屏展示幻灯片，支持：

- 点击或空格键翻页
- 演讲者视图（当前页+下一页预览+备注+计时器）
- 激光笔（鼠标变为红点）
- 画笔标注（临时标注，不保存到数据）

---

## 4 数据输入方式设计

每种展示方式都支持编辑，编辑操作直接修改原始数据。以下详细说明各种输入方式的交互逻辑。

---

### 4.1 大纲模式输入

大纲模式是最主要的输入方式，操作逻辑参考幕布。

**新增节点**：

- **Enter 键**：在当前节点下方新增同级节点。系统自动生成键名，如当前在 t2-1 下按 Enter，自动创建 t2-2
- **Tab 键**：将当前节点降级为上方节点的子节点。如 t2-2 按 Tab 后变为 t2-1 的子节点 t3-1
- **Shift+Tab**：将当前节点升级。如 t3-1 按 Shift+Tab 后变为 t2-2

**自动键名生成规则**：

- 新增同级节点：取同级最大序号+1（例如已有 t2-1, t2-2，新增为 t2-3）
- 降级节点：在目标父节点下取子节点最大序号+1（例如 t2-1 下已有 t3-1，新增为 t3-2）
- 升级节点：在目标层级取最大序号+1
- 删除节点后序号不回收，避免引用失效

**自动创建的字段**：

- `content: ""`（空字符串，等待用户输入）
- `hide: 0`（默认展开）
- `hide_body: 0`（默认 body 可见）
- 字体/颜色等字段不创建，继承 type_global 的值

---

### 4.2 思维导图模式输入

在思维导图中直接编辑节点内容，操作同步到原始数据。

**新增节点**：

- **Enter**：新增同级节点（在当前节点下方）
- **Tab**：新增子节点（在当前节点右侧展开）
- 拖拽节点到另一个节点上：改变父子关系

**编辑内容**：

- 双击节点进入编辑模式，修改 content 字段
- 节点样式修改（颜色、形状等）通过右键菜单或工具栏

**与大纲模式的区别**：

- 思维导图更适合发散性思维，快速创建多个分支
- 支持自由拖拽调整结构
- body 正文默认折叠（`hide_body` 默认为 1），可点击展开指示器显示

---

### 4.3 文档模式输入

文档模式的输入体验接近 Word，支持富文本编辑。

**标题输入**：

- 在标题行末尾按 Enter：新增同级标题
- 在正文中按 Enter：普通换行（不创建新节点）
- Tab/Shift+Tab：调整标题层级

**正文输入**：

- 在标题下方直接输入正文，写入 body 字段
- 支持富文本格式：选中文字后通过工具栏设置格式
- **选中部分文字设置格式时，自动生成逐字富文本值**（如选中几个字加粗，自动将 bold 字段转为逐字语法）
- 格式修改自动同步到原始数据对应字段

**媒体插入**：

- 通过插入菜单或拖拽文件插入图片/视频
- 自动生成 `{{image:...}}` 语法写入 content 或 body

---

### 4.4 表格模式输入

表格模式的输入方式与 Excel 类似，但增加了数据路径表达式。

**单元格输入**：

- 直接输入文本：作为静态内容
- 输入数据路径表达式（如 `t1-*.content`）：自动提取原始数据
- 修改已填充的单元格：如果该单元格由表达式生成，修改后同步更新原始数据中对应节点的字段值

**表格结构操作**：

- 右键插入/删除行列
- 拖拽调整列宽行高
- 合并/拆分单元格

**公式栏**：

- 表格上方显示公式栏，显示当前单元格的表达式或值
- 可在公式栏中编辑表达式

---

### 4.5 幻灯片模式输入

幻灯片模式的输入侧重于页面布局和内容编排。

**内容编辑**：

- 双击页面中的文本区域进入编辑模式
- 编辑内容同步修改原始数据中对应节点的 content 或 body
- 支持富文本格式设置（含逐字富文本）

**布局调整**：

- 通过工具栏切换页面布局模板
- 拖拽调整内容区域的大小和位置
- 右键菜单设置页面背景、切换动画

**新增页面**：

- 在缩略图区域右键"新增页面"
- 自动在原始数据中创建对应层级的新节点

---

### 4.6 侧边栏原始数据编辑

所有展示方式都支持打开侧边栏，直接编辑原始数据。

**侧边栏功能**：

- 以 JSON 树形结构显示当前选中节点的原始数据
- 可直接修改任何字段的值（包括逐字富文本值）
- 修改后实时同步到当前展示方式的渲染
- 支持添加/删除字段
- 语法高亮和自动补全

**使用场景**：

- 精确调整格式参数（如精确的 RGB 颜色值）
- 手动编写逐字富文本值
- 批量修改属性
- 查看和编辑在当前展示方式中不可见的字段（如 note、comment）
- 调试和排查显示问题

---

## 5 产品形态与交互设计

---

### 5.1 多端支持

本软件支持三种客户端形态：

**桌面端（Windows / macOS / Linux）**：

- 使用 Electron 或 Tauri 框架构建
- 完整功能支持，包括所有五种展示方式
- 本地文件读写，支持离线使用
- 系统级快捷键支持
- 文件关联（双击 .udd 文件直接打开）

**网页端**：

- 基于浏览器运行，无需安装
- 完整功能支持（与桌面端一致）
- 通过云端存储实现跨设备访问
- 支持链接分享和协作编辑

**移动端（iOS / Android）**：

- 使用 React Native 或 Flutter 构建
- 优化触屏交互（手势缩放、滑动切换）
- 大纲模式和文档模式为主要编辑方式
- 思维导图模式支持手势缩放和平移
- 幻灯片模式支持全屏演示
- 表格模式简化为只读浏览（复杂编辑引导到桌面端）

---

### 5.2 界面布局

主界面分为以下区域：

```
┌──────────────────────────────────────────────────┐
│  菜单栏：文件 | 编辑 | 视图 | 插入 | 格式 | 帮助  │
│──────────────────────────────────────────────────│
│  工具栏：字体|字号|加粗|斜体|颜色|对齐|插入|...   │
│──────────────────────────────────────────────────│
│  展示方式切换栏：大纲 | 思维导图 | 文档 | 表格 | 幻灯片 │
├──────────────────────────────────┬───────────────┤
│                                  │               │
│                                  │  侧边栏       │
│         主编辑区域                │  （原始数据）  │
│                                  │               │
│                                  │               │
├──────────────────────────────────┴───────────────┤
│  状态栏：字数统计 | 节点数 | 当前层级 | 缩放比例   │
└──────────────────────────────────────────────────┘
```

侧边栏默认隐藏，可通过快捷键 `Ctrl+\` 或菜单切换显示。

---

### 5.3 用户使用流程

**首次使用**：

1. 打开软件，创建新文件或打开已有文件
2. 打开已有文件时，触发底层数据→原始数据的转换
3. 默认进入大纲模式
4. 用户开始输入内容

**日常编辑流程**：

1. 打开文件（自动转换为原始数据）
2. 在任意展示方式下编辑内容
3. 随时切换展示方式查看效果
4. 可打开侧边栏精确调整格式
5. 保存文件（自动压缩为底层数据）
6. 可导出为原始数据 JSON、PDF、图片等格式

---

### 5.4 快捷键汇总

**通用快捷键（所有展示方式）**：

| 快捷键 | 功能 |
|--------|------|
| Ctrl+S | 保存文件 |
| Ctrl+Z | 撤销 |
| Ctrl+Shift+Z | 重做 |
| Ctrl+C/V/X | 复制/粘贴/剪切 |
| Ctrl+F | 搜索 |
| Ctrl+H | 替换 |
| Ctrl+\\ | 切换侧边栏 |
| Ctrl+1~5 | 切换展示方式（1=大纲, 2=思维导图, 3=文档, 4=表格, 5=幻灯片） |
| Ctrl+N | 新建文件 |
| Ctrl+O | 打开文件 |
| Ctrl+P | 打印/导出PDF |

**大纲/文档模式快捷键**：

| 快捷键 | 功能 |
|--------|------|
| Enter | 新增同级节点 |
| Tab | 降级节点 |
| Shift+Tab | 升级节点 |
| Ctrl+B | 加粗 |
| Ctrl+I | 斜体 |
| Ctrl+U | 下划线 |

**思维导图模式快捷键**：

| 快捷键 | 功能 |
|--------|------|
| Enter | 新增同级节点 |
| Tab | 新增子节点 |
| 空格键 | 折叠/展开 |
| Ctrl+加号 | 放大画布 |
| Ctrl+减号 | 缩小画布 |
| Ctrl+0 | 重置缩放 |

**表格模式快捷键**：

| 快捷键 | 功能 |
|--------|------|
| Tab | 移动到右侧单元格 |
| Shift+Tab | 移动到左侧单元格 |
| Enter | 移动到下方单元格 |
| F2 | 进入单元格编辑模式 |
| Delete | 清除单元格内容 |
| Ctrl+; | 插入当前日期 |
| Ctrl+Shift+; | 插入当前时间 |
| Alt+Enter | 单元格内换行 |

**幻灯片模式快捷键**：

| 快捷键 | 功能 |
|--------|------|
| F5 | 全屏演示 |
| Esc | 退出演示 |
| 左/右箭头 | 上一页/下一页 |
| 空格键 | 下一页（演示模式下） |
| B | 黑屏（演示模式下，再按恢复） |
| W | 白屏（演示模式下，再按恢复） |

---

### 5.5 主题与外观

系统内置多套界面主题，用户可自由切换：

**内置主题**：

| 主题 | 说明 |
|------|------|
| 浅色主题（默认） | 白色背景，深色文字，适合日常办公 |
| 深色主题 | 深灰背景，浅色文字，适合夜间使用或长时间编辑 |
| 护眼主题 | 淡绿色背景，降低蓝光，减轻视觉疲劳 |
| 高对比度主题 | 黑白高对比，适合视力辅助需求 |

**自定义主题**：

- 用户可自定义界面配色（菜单栏、工具栏、编辑区背景色）
- 主题配置保存在用户配置文件中，跨项目生效
- 主题仅影响软件界面外观，不影响文档内容的渲染样式

**缩放设置**：

- 界面缩放：50% ~ 200%，影响整体 UI 大小
- 内容缩放：独立于界面缩放，仅影响编辑区内容显示大小
- Ctrl+滚轮 快速调整内容缩放
- Ctrl+0 重置为 100%

---

### 5.6 撤销/重做机制

系统采用操作栈实现撤销/重做，记录粒度为单次用户操作。

**操作记录规则**：

- 每次键盘输入（连续输入合并为一次操作，暂停超过 1 秒则分割）
- 每次拖拽操作（移动节点、调整大小等）
- 每次格式修改（字体、颜色、对齐等，含逐字富文本值的变更）
- 每次结构变更（新增/删除/移动节点）

**撤销栈容量**：

- 默认保存最近 100 步操作
- 用户可在设置中调整（范围 20 ~ 500）
- 关闭文件后撤销栈清空（不持久化）

**跨展示方式撤销**：

- 在大纲模式下的操作，切换到文档模式后仍可撤销
- 撤销栈是全局的，不区分展示方式
- 撤销操作会自动切换回操作发生时的展示方式（可在设置中关闭此行为）

---

## 6 文件格式与存储设计

---

### 6.1 文件格式定义

本系统使用自定义文件格式 `.udd`（Unified Data Document）作为默认存储格式。

`.udd` 文件本质上是一个 ZIP 压缩包，内部包含以下结构：

```
文件名.udd（ZIP 压缩包）
├── data.json           ← 底层数据（经转义表压缩后的 JSON）
├── meta.json           ← 文件元信息（标题、作者、创建时间等）
├── view_state.json     ← 视图状态（上次使用的展示方式、滚动位置、缩放比例等）
├── escape_table.json   ← 自定义转义表（可选，仅当项目有自定义映射时存在）
├── assets/             ← 媒体资源目录
│   ├── images/         ← 图片文件
│   ├── videos/         ← 视频文件（仅存储本地嵌入的视频）
│   └── audio/          ← 音频文件
└── thumbnails/         ← 缩略图目录（用于快速预览）
    └── preview.png     ← 文件预览缩略图
```

**各文件说明**：

**data.json**：

- 存储底层数据（压缩后的键值对结构）
- 打开文件时通过转义表还原为原始数据
- 保存文件时从原始数据压缩生成
- 使用 UTF-8 编码，无 BOM

**meta.json**：

- 独立于 data.json 存储，便于文件管理器快速读取文件信息
- 无需解压整个文件即可获取标题、作者、修改时间等
- 结构示例：

```json
{
  "format_version": "1.0",
  "app_version": "1.0.0",
  "title": "文档标题",
  "author": "作者名",
  "created": "2026-02-11T10:00:00Z",
  "modified": "2026-02-11T14:00:00Z",
  "node_count": 42,
  "word_count": 3500,
  "checksum": "sha256:abcdef..."
}
```

**view_state.json**：

- 记录用户上次关闭文件时的视图状态
- 重新打开时恢复到上次的浏览位置和展示方式
- 结构示例：

```json
{
  "last_view": "outline",
  "outline_scroll": 120,
  "outline_collapsed": ["t1-2", "t1-3.t2-1"],
  "mindmap_zoom": 0.8,
  "mindmap_pan": {"x": -50, "y": 30},
  "document_scroll": 450,
  "document_page": 3,
  "table_scroll": {"row": 0, "col": 0},
  "slide_current": 2,
  "sidebar_visible": false,
  "selected_node": "t1-1.t2-1"
}
```

**assets 目录**：

- 媒体文件统一存放在 assets 子目录中
- 原始数据中的媒体路径使用相对路径引用（如 `"assets/images/goal.png"`）
- 插入媒体时自动复制文件到 assets 目录并重命名（避免文件名冲突）
- 重命名规则：原始文件名 + 时间戳哈希，如 `"goal_a3f2b1.png"`
- 删除节点时不自动删除关联媒体（防止误删），提供"清理未引用资源"功能

---

### 6.2 自动保存与备份

**自动保存机制**：

- 默认每 60 秒自动保存一次（用户可在设置中调整间隔：10秒 ~ 5分钟）
- 仅在有修改时触发保存，无修改不写盘
- 自动保存写入临时文件（.udd.tmp），写入成功后替换原文件
- 若写入过程中断电或崩溃，下次打开时自动检测并恢复临时文件
- 自动保存过程在后台线程执行，不阻塞用户编辑

**版本备份机制**：

- 每次手动保存（Ctrl+S）时，自动创建一个版本快照
- 快照存放在项目目录下的 `.udd_history/` 隐藏文件夹中
- 快照命名格式：`文件名_YYYYMMDD_HHmmss.udd`
- 默认保留最近 10 个版本快照（用户可调整）
- 超出数量限制时，自动删除最旧的快照
- 用户可通过"文件 → 版本历史"查看和恢复历史版本

**崩溃恢复机制**：

- 软件异常退出后，下次启动时自动检测未保存的临时文件
- 弹出恢复对话框，用户可选择：
  - a) 恢复到崩溃前的状态（使用临时文件）
  - b) 恢复到上次手动保存的状态（使用原文件）
  - c) 同时打开两者进行对比

---

### 6.3 导入功能

系统支持从以下格式导入数据，转换为原始数据结构：

**Markdown 导入（.md）**：

- `#` 标题 → t1 节点，`##` 标题 → t2 节点，以此类推
- 标题下方的段落文字 → body 字段
- 图片语法 `![alt](path)` → `{{image:path}}` 内嵌媒体
- 链接语法 `[text](url)` → `{{link:url, link.text:text}}`
- 加粗/斜体等格式 → 对应的 bold/italic 字段
- 无序列表 → 子节点，numbering_style 设为 `"bullet"`
- 有序列表 → 子节点，numbering_style 设为 `"1.1.1"`

**Word 导入（.docx）**：

- 使用第三方库解析 docx 文件
- 标题样式（Heading 1~6）→ t1~t6 节点
- 正文段落 → body 字段
- 字体、字号、颜色、加粗等格式 → 对应字段
- **局部富文本格式**（如一段文字中部分加粗）→ 自动转换为逐字富文本语法
- 段落间距、缩进、对齐 → 对应段落属性字段
- 嵌入图片 → 提取到 assets 目录，生成内嵌媒体语法
- 表格 → 暂不支持（提示用户在表格模式中重建）

**OPML 导入（.opml）**：

- OPML 是大纲类工具的通用交换格式
- `<outline>` 节点层级 → t 节点层级
- text 属性 → content 字段
- _note 属性 → body 字段

**纯文本导入（.txt）**：

- 按缩进层级识别结构（Tab 或空格缩进）
- 每行文字作为一个节点的 content
- 无缩进 → t1，一级缩进 → t2，以此类推
- 若无明显缩进结构，整体作为单个 t1 节点的 body

**JSON 导入（.json）**：

- 直接作为原始数据加载（需符合本系统的数据结构规范）
- 自动校验必要字段，缺失字段补充默认值
- 不符合规范的 JSON 提示错误并标注问题位置

---

### 6.4 导出功能

系统支持将原始数据导出为以下格式：

**PDF 导出**：

- 基于文档模式的渲染结果生成 PDF
- 完整保留字体、颜色、段落格式、图片等排版效果（含逐字富文本样式）
- 支持设置页面大小（A4、A3、Letter 等）、页边距、页眉页脚
- 支持生成目录页（基于标题层级自动生成）
- 支持添加页码（位置：页脚居中/右下角，格式：数字/罗马数字）
- 幻灯片模式也可导出为 PDF（每页幻灯片一页 PDF）

**Markdown 导出（.md）**：

- t1 → `#` 标题，t2 → `##` 标题，以此类推
- body → 标题下方的段落文字
- bold/italic → `**加粗**` / `*斜体*` 语法
- 逐字富文本格式 → 尽可能用 Markdown 行内语法表达（如部分加粗），无法精确表达的样式丢弃
- 内嵌图片 → `![说明](路径)` 语法
- 内嵌链接 → `[文字](URL)` 语法
- 格式属性（颜色、字号等）在 Markdown 中无法表达，导出时丢弃

**Word 导出（.docx）**：

- t1~t6 → Heading 1~6 样式
- body → 正文段落
- 字体、字号、颜色、加粗等 → Word 对应格式
- 逐字富文本 → Word 的 Run 级别格式（精确映射）
- 段落间距、缩进、对齐 → Word 段落格式
- 内嵌图片 → Word 嵌入图片
- 使用第三方库（如 docx.js）生成标准 docx 文件

**PowerPoint 导出（.pptx）**：

- 基于幻灯片模式的分页和布局结果生成
- 每页幻灯片 → 一页 PPT
- 标题、正文、图片按布局模板定位
- 背景色/背景图 → PPT 页面背景
- 切换动画 → PPT 切换效果（尽可能映射到 PPT 内置动画）
- 演讲者备注 → PPT 备注区

**图片导出（PNG / SVG）**：

- 思维导图模式 → 导出为完整的思维导图图片
- 支持 PNG（位图，适合分享）和 SVG（矢量，适合印刷）
- 可设置导出分辨率（1x、2x、3x）
- 可设置背景色（透明/白色/自定义）
- 可选择导出范围（全部节点/选中的子树）

**OPML 导出（.opml）**：

- 用于与其他大纲工具交换数据
- t 节点层级 → `<outline>` 层级
- content → text 属性
- body → _note 属性

**原始数据导出（.json）**：

- 导出完整的原始数据 JSON（未经转义表压缩）
- 适合开发者调试、数据迁移、第三方工具集成
- 可选择是否包含 type_global 和 meta 信息

---

### 6.5 转义表管理

转义表是原始数据与底层数据之间的桥梁，需要妥善管理。

**默认转义表**：

- 随软件安装，存放在安装目录的 `config/escape_table.json` 中
- 包含所有系统内置字段的映射（如 content→c, body→b 等）
- 软件更新时自动合并新增字段，不覆盖已有映射

**自定义转义表**：

- 用户可在项目根目录放置 `escape_table.json` 覆盖默认映射
- 自定义表与默认表合并，自定义优先
- 适用场景：用户自定义了新字段，需要为其分配短键名

**转义表版本管理**：

- 转义表包含 version 字段，标识当前版本
- 打开旧版本文件时，自动检测转义表版本差异
- 若旧文件使用的转义表缺少新字段映射，自动补充
- 向后兼容：新版软件始终能打开旧版文件

---

### 6.6 搜索与替换

**全局搜索（Ctrl+F）**：

- 在当前文件的所有节点中搜索文本
- 搜索范围包括 content、body、note、comment 字段
- 支持精确匹配和模糊匹配
- 支持正则表达式搜索
- 搜索结果高亮显示，支持上/下跳转
- 在不同展示方式下均可使用，搜索结果定位到对应节点

**全局替换（Ctrl+H）**：

- 在搜索基础上支持逐个替换或全部替换
- 替换前预览所有匹配项，用户可勾选需要替换的项
- 替换操作记入撤销栈，可一键撤销全部替换
- 替换涉及逐字富文本值时，自动调整字符范围偏移量

**跨文件搜索**：

- 在项目文件夹中搜索所有 .udd 文件
- 搜索结果按文件分组显示
- 双击搜索结果跳转到对应文件的对应节点

**节点定位**：

- 通过键名路径快速定位节点（如输入 `"t1-2.t2-1"` 跳转到对应节点）
- 通过标题内容模糊搜索定位节点
- 在大纲模式下支持目录导航面板

---

### 6.7 打印设计

打印功能基于文档模式或幻灯片模式的渲染结果输出。

**文档打印**：

- 基于文档模式渲染，按页面设置分页输出
- 页面设置：纸张大小（A4/A3/Letter/自定义）、方向（纵向/横向）、页边距
- 页眉页脚：支持插入页码、日期、文档标题、作者名
- 打印范围：全部页面 / 指定页码范围 / 选中内容
- 打印预览：所见即所得，可在预览中调整页面设置

**幻灯片打印**：

- 每页幻灯片打印为一页纸
- 支持讲义模式：每页纸打印 2/4/6/9 张幻灯片缩略图
- 可选择是否打印演讲者备注

**大纲打印**：

- 按大纲模式的缩进层级打印
- 可设置打印深度（如只打印到2级标题）
- 可选择是否打印 body 正文内容

---

## 7 多端协同与未来规划

---

### 7.1 多端数据同步

本系统支持桌面端、网页端、移动端三端协同，数据通过云端同步。

**同步架构**：

- 采用"本地优先 + 云端同步"模式
- 编辑操作始终在本地执行，保证离线可用
- 联网时自动将变更同步到云端
- 其他设备从云端拉取最新变更

**同步粒度**：

- 以节点为最小同步单位（而非整个文件）
- 修改 t1-1.t2-1 的 content 时，仅同步该节点的变更
- 减少网络传输量，提高同步效率

**同步协议**：

每次修改生成一条变更记录（Change Record）：

```json
{
  "id": "uuid",
  "timestamp": "2026-02-11T14:30:00Z",
  "device_id": "device-abc",
  "user_id": "user-123",
  "path": "t1-1.t2-1.content",
  "action": "update",
  "old_value": "旧标题",
  "new_value": "新标题"
}
```

变更记录按时间戳排序，顺序应用到各端。

**冲突处理**：

不同设备同时修改同一节点的同一字段时产生冲突。冲突解决策略（按优先级）：

1. **自动合并**：若修改的是不同字段（如 A 改 content，B 改 color），自动合并
2. **最后写入优先**：同一字段冲突时，时间戳较晚的修改生效
3. **手动解决**：用户可在"同步冲突"面板中查看冲突详情，手动选择保留哪个版本

冲突发生时不丢弃任何数据，被覆盖的版本保存在冲突历史中。

---

### 7.2 实时协作编辑

多人可同时编辑同一份文档，实时看到彼此的操作。

**协作机制**：

- 基于 OT（Operational Transformation）或 CRDT 算法实现实时协作
- 每个用户的光标位置和选区实时同步显示
- 不同用户的光标用不同颜色标识，旁边显示用户名

**协作权限**：

| 级别 | 能力 |
|------|------|
| 所有者 | 完全控制，可删除文档、管理协作者 |
| 编辑者 | 可编辑所有内容，不可删除文档或管理权限 |
| 评论者 | 只读 + 可添加评论（comment 字段） |
| 查看者 | 纯只读，不可修改任何内容 |

**协作功能**：

- 评论与讨论：选中任意内容添加评论，支持 @提及 其他协作者
- 修改建议：评论者可提出修改建议，编辑者可接受或拒绝
- 在线状态：显示当前在线的协作者列表
- 操作历史：记录每位协作者的修改历史，可按用户筛选查看

---

### 7.3 分享与发布

**链接分享**：

- 生成文档的分享链接，接收者无需注册即可查看
- 分享链接可设置权限（查看/评论/编辑）
- 可设置链接有效期（永久/7天/30天/自定义）
- 可设置访问密码
- 可随时撤销分享链接

**嵌入分享**：

- 生成 iframe 嵌入代码，可嵌入到网页、博客、Wiki 中
- 嵌入视图支持选择展示方式（如仅嵌入思维导图视图）
- 嵌入内容只读，实时同步源文档的更新

**发布为网页**：

- 将文档发布为独立网页，生成公开访问的 URL
- 支持自定义域名绑定
- 发布后的网页支持五种展示方式切换（由访问者自行选择）
- 可设置 SEO 信息（标题、描述、关键词）

---

### 7.4 模板系统

**系统内置模板**：

| 模板 | 预设结构 |
|------|----------|
| 会议记录 | 会议主题、参会人员、议题、决议、待办事项 |
| 项目计划 | 项目概述、里程碑、任务分解、风险评估 |
| 读书笔记 | 书名信息、章节摘要、读后感、金句摘录 |
| 周报/日报 | 本周完成、下周计划、问题与风险 |
| 产品需求文档 | 背景、目标、功能需求、非功能需求、排期 |
| 空白模板 | 无预设内容 |

**模板包含内容**：

- 预设的标题层级结构（t1/t2/t3 节点）
- 预设的 type_global 样式（字体、字号、颜色方案）
- 预设的展示方式偏好（如会议记录默认大纲模式，项目计划默认思维导图模式）
- 占位提示文字（灰色显示，用户输入后自动消失）

**自定义模板**：

- 用户可将任意文档"另存为模板"
- 自定义模板保存在用户配置目录中
- 支持模板的导入/导出/分享

---

### 7.5 插件与扩展机制

系统提供插件 API，允许第三方开发者扩展功能。

**插件能力范围**：

- 自定义展示方式：在五种内置展示方式之外添加新的渲染方式
- 自定义数据路径函数：为表格模式添加新的表达式函数
- 自定义导入/导出格式：支持更多文件格式的转换
- 自定义工具栏按钮：在工具栏中添加自定义操作
- 自定义快捷键：注册新的快捷键绑定

**插件 API 设计**：

插件以 JavaScript/TypeScript 模块形式加载，提供标准化的 API 接口：

| API | 能力 |
|-----|------|
| DataAPI | 读写原始数据（增删改查节点、修改字段值） |
| ViewAPI | 控制展示方式（注册渲染器、监听视图切换） |
| UIAPI | 操作界面元素（添加菜单项、工具栏按钮、侧边栏面板） |
| EventAPI | 监听系统事件（节点变更、文件保存、展示方式切换） |

**插件安全**：

- 插件运行在沙箱环境中，不可直接访问文件系统
- 插件需声明所需权限（数据读写、网络访问、文件访问等）
- 用户安装插件时需确认权限授予
- 官方插件市场提供审核机制，确保插件安全性

---

### 7.6 AI 智能辅助

系统集成 AI 能力，辅助用户高效创作和整理内容。

**智能生成**：

- 输入主题关键词，AI 自动生成大纲结构（标题层级 + 要点）
- 选中标题节点，AI 自动扩写 body 正文内容
- 选中多个节点，AI 自动生成摘要总结

**智能排版**：

- AI 分析文档内容，自动推荐合适的 type_global 样式方案
- 幻灯片模式下，AI 自动推荐每页的最佳布局模板
- 思维导图模式下，AI 自动推荐最佳布局类型和配色方案

**智能整理**：

- AI 分析大纲结构，建议节点的合并、拆分、重新排序
- AI 检测重复内容，提示用户合并或使用引用
- AI 检查标题层级是否合理（如二级标题下直接出现四级标题）

**智能转换**：

- 粘贴纯文本时，AI 自动识别结构并转换为标题层级
- 粘贴表格数据时，AI 自动识别行列关系并生成节点结构
- 导入外部文件时，AI 辅助修正格式映射中的歧义

---

### 7.7 性能优化策略

**大文件处理**：

- 节点数超过 1000 时，启用虚拟滚动（仅渲染可视区域内的节点）
- 思维导图模式下，超出画布可视范围的节点延迟渲染
- 幻灯片模式下，仅渲染当前页和前后各一页，其余页面按需加载
- 媒体资源懒加载：图片/视频在进入可视区域时才加载

**数据操作优化**：

- 编辑过程中操作原始数据（内存中的 JSON 对象），不触发转义压缩
- 仅在保存文件时执行一次原始数据→底层数据的转义压缩
- 节点变更时仅重新渲染受影响的节点，不全量刷新
- 样式变更时使用 diff 算法，仅更新变化的 CSS 属性
- 逐字富文本解析结果缓存，字段值未变时不重新解析

**展示方式切换优化**：

- 切换展示方式时不重新解析数据，仅切换渲染器
- 每种展示方式的渲染器预加载，切换时无需等待初始化
- 缓存各展示方式的渲染状态，切换回来时恢复上次的滚动位置和缩放

---

### 7.8 无障碍访问

系统遵循 WCAG 无障碍设计原则，确保残障用户也能正常使用。

**键盘无障碍**：

- 所有功能均可通过键盘操作完成，不依赖鼠标
- Tab 键可在界面元素间顺序导航
- 焦点指示器清晰可见（高亮边框）

**屏幕阅读器支持**：

- 所有界面元素提供语义化标签（ARIA 属性）
- 节点层级关系通过 ARIA tree 角色表达
- 展示方式切换、工具栏操作均有语音提示

**视觉辅助**：

- 高对比度主题（见 5.5 节）
- 字号可自由调整，最大支持 200% 缩放
- 颜色不作为传达信息的唯一手段（配合图标或文字标签）

---

### 7.9 国际化与本地化

**语言支持**：

- 界面语言：简体中文（默认）、繁体中文、英文、日文
- 界面语言可在设置中切换，切换后立即生效，无需重启
- 文档内容语言由 `meta.language` 字段标识，影响拼写检查和排版规则

**排版适配**：

- 中文排版：支持首行缩进、中文标点挤压、中西文混排间距
- 英文排版：支持连字符断词（hyphenation）、两端对齐
- 日文排版：支持纵书（竖排）模式
- RTL 语言：支持从右到左的文本方向（阿拉伯语、希伯来语）

---

### 7.10 版本迭代规划

**第一阶段（MVP）**：

- 实现原始数据模型和转义表机制
- 实现大纲模式（完整编辑功能）
- 实现文档模式（基础排版渲染）
- 实现 .udd 文件格式的读写
- 网页端（离线优先，PWA），支持手机浏览器打开
- 使用 File System Access API 实现本地 .udd 文件读写
- IndexedDB 作为本地缓存

**第二阶段**：

- 实现思维导图模式（多种布局类型）
- 实现表格模式（数据路径表达式）
- 完善文档模式（完整段落格式、分页渲染、逐字富文本渲染）
- Markdown 导入/导出
- Word 导入/导出
- PDF 导出
- 自动保存与版本备份
- 搜索与替换功能

**第三阶段**：

- 实现幻灯片模式（布局模板、演示模式）
- PowerPoint 导出
- 移动端开发（iOS / Android）
- 云端存储与多端同步
- 模板系统

**第四阶段**：

- 实时协作编辑
- 分享与发布功能
- 桌面端开发
- 插件与扩展机制
- AI 智能辅助功能

**第五阶段**：

- 国际化与多语言支持
- 无障碍访问完善
- 性能深度优化（超大文件支持）
- 插件市场上线
- 企业版功能（团队管理、权限体系、审计日志）

---

## 附录：术语表

| 术语 | 说明 |
|------|------|
| 原始数据 | 用户可直接阅读的嵌套键值对 JSON 结构 |
| 底层数据 | 经转义表压缩后的存储格式 |
| 转义表 | 原始数据键名与底层数据短键名之间的双向映射字典 |
| 节点 | 原始数据中的一个标题单元（如 t1-1、t2-1） |
| 键名 | 节点的标识符（如 t1-1、t2-1.t3-1） |
| content | 节点的标题文字 |
| body | 节点的正文内容 |
| type_global | 全局样式定义区域 |
| 展示方式 | 数据的渲染形态（大纲、思维导图、文档、表格、幻灯片） |
| .udd | 本系统的默认文件格式（Unified Data Document） |
| 数据路径表达式 | 表格模式中用于从原始数据提取值的语法 |
| 内嵌媒体语法 | 在 content/body 中使用 `{{...}}` 嵌入图片/视频/音频的语法 |
| 逐字富文本语法 | 字符级样式字段的值格式，支持对特定字符范围设置不同样式（如 `"1;0(2,3)(5,10)"`） |
| hide | 子节点折叠控制字段 |
| hide_body | body 正文折叠控制字段 |
